<%= simple_form_for(@city) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :state %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
